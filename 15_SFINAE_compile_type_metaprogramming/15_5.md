# is_constructible
***
````c++
template <typename T, typename... Args>
const bool is_contructible_v = is_contructible<T, Args...>::value;


template <typename T>
class is_copy_constructible{
private:
    
    static auto f(int) -> deltype(TT(declval<const TT&>()), int())
public:
};
````
